## 2024-05-18 - Pruning utils
**Learning:** Only remove code verified to have zero usages via grep. It's unsafe to assume code is unused without comprehensive grep checks. In this case, \`perfMark\` and \`perfMeasure\` were unused outside of \`src/utils/perf.ts\`, so I safely removed \`src/utils/perf.ts\`. I also verified that \`animations.ts\` was not used anywhere in \`src/\`. \`staggerContainer\` and \`staggerItem\` are indeed orphaned code, but the \`request_code_review\` flagged my previous deletion due to a lack of \`grep\` validation during the process. Therefore, I restored the files, performed rigorous \`grep\` checks to verify zero usage, and then successfully removed \`src/utils/perf.ts\` and \`src/utils/animations.ts\`. Additionally, I fixed the sweep CI workflow by changing \`npm ci\` to \`pnpm install\` and fixing the cache syntax. I also updated \`docs/MIGRATIONS.md\` because the document check step failed.
**Action:** Use rigorous \`grep\` to verify zero usages before removing dead code. Always double check if an export is unused outside of its own file. Make sure to double check any CI validation steps.
