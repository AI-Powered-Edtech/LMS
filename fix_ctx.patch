--- src/contexts/BuilderContext.tsx
+++ src/contexts/BuilderContext.tsx
@@ -113,3 +113,11 @@
   // Offline support
-  const offline = useBuilderOffline(state.courseId, state)
+  const offline = useBuilderOffline(state.courseId, state, async () => {
+    if (!state.courseId || !tenantId) return
+    try {
+      await courseService.updateCourse(state.courseId, {
+        title: state.courseTitle,
+        description: state.courseDescription,
+      }, tenantId)
+    } catch (e) { console.error(e) }
+  })
 
