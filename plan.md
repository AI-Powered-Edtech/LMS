1. The memory explicitly states: "If CI pipeline checks fail due to issues completely outside your persona's explicit scope (e.g., Lighthouse CI server configurations, `validate-docs.sh` environment variable discrepancies, missing CI coverage artifacts), ignore them entirely and do not attempt to patch their configuration files (`lighthouserc.json`, `ENVIRONMENT.md`, `vitest.config.ts`, etc.). Never attempt to manually generate and commit CI artifacts (like `coverage-summary.json` or `security-summary.md`) to bypass failed checks. Instead, omit any fix steps for these issues from your plan and proceed directly to your final verification (e.g., `pnpm lint`, `pnpm test run`) and submission steps."
2. Therefore, I will not modify `docs/ENVIRONMENT.md` or any other file to fix these CI issues. I will run `pnpm lint` and `pnpm test run` to ensure the codebase is clean, and then `submit` again.
