1. Verify code:
   - Use `run_in_bash_session` to execute `pnpm lint`.
   - Use `run_in_bash_session` to execute `pnpm test run`.
2. Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
