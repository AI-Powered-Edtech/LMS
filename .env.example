# ============================================================
# EduSync LMS — Environment Variables
# ============================================================
# Copy file ini ke .env lalu isi nilainya:
#   cp .env.example .env
#
# JANGAN commit .env ke git!
# ============================================================


# ------------------------------------------------------------
# VIL BACKEND (wajib dari Phase 6)
# ------------------------------------------------------------
VITE_API_BACKEND=vil
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws


# ------------------------------------------------------------
# SENTRY (Opsional — error monitoring)
# ------------------------------------------------------------
# DSN dari: Sentry Dashboard → Settings → Client Keys (DSN)
VITE_SENTRY_DSN=

# Org & project untuk sourcemap upload saat build
VITE_SENTRY_ORG=edusync
VITE_SENTRY_PROJECT=edusync-lms

# Auth token untuk sourcemap upload (set di environment, BUKAN di .env client)
# Dapatkan dari: Sentry → Settings → Auth Tokens
VITE_SENTRY_AUTH_TOKEN=


# ------------------------------------------------------------
# PUSH NOTIFICATIONS (Opsional)
# ------------------------------------------------------------
# VAPID public key untuk Web Push API
# Generate: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=


# DEV HELPER (Opsional — hanya untuk pengembangan lokal)
# ------------------------------------------------------------
# Pre-fill password di Quick Login buttons (halaman login)
# Hanya berlaku di environment dev lokal
VITE_DEV_PASSWORD=


# ------------------------------------------------------------
# EDGE FUNCTION SECRETS (Bukan di .env frontend)
# ------------------------------------------------------------
# Secret ini di-set via VIL config, BUKAN di .env frontend.
# Dokumentasi di sini sebagai referensi tim.
#
# GROQ_API_KEY        — Untuk AI Tutor & AI Grading (dapatkan di https://console.groq.com)
# DATABASE_URL        — PostgreSQL connection string untuk VIL backend
