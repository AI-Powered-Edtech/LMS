- [x] Login mengembalikan semua membership tenant+role user (tanpa `LIMIT 1`) dan menyertakan `active_tenant_id`.
- [x] Access token selalu memiliki claim `tenant_id` (active tenant) dan `role` sesuai tenant aktif.
- [x] Endpoint `POST /api/v1/auth/switch-tenant` tersedia, memvalidasi membership, dan mengembalikan token baru untuk tenant target.
- [x] Frontend tenant switch memanggil endpoint switch-tenant dan mengganti session/token aktif.
- [x] Layout dan Sidebar tidak lagi memakai fallback `activeRole ?? role` dan tidak merender layout yang salah saat bootstrap.
- [x] `main.rs` hard-fail bila `JWT_SECRET` tidak ada atau < 32 karakter.
- [x] `observer(true)` hanya aktif bila `ENABLE_OBSERVER=true`.
- [x] Tidak ada `console.log` user state di AuthContext (dan log sensitif serupa).
- [x] `TooManyRequests` mengembalikan HTTP 429 (brute-force lock + mapping error).
- [x] Semua komentar legacy “Supabase” di kode telah dihapus.
- [x] `.pnpm-store/` dihapus dari repo dan sudah masuk `.gitignore`.
- [x] `db: any` sudah diganti menjadi typed facade tanpa mengubah behavior consumer.
- [x] Tidak ada UUID fallback berbasis `Math.random()` untuk ID yang bersifat UUID.
