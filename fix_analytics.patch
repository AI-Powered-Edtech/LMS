--- src/pages/Analytics.tsx
+++ src/pages/Analytics.tsx
@@ -9,2 +9,3 @@
 import { LiveActivityFeed } from '@/src/features/analytics/components/LiveActivityFeed'
+import { useRoleBasedPath } from '@/src/hooks/useRoleBasedPath'
 import { usePageTitle } from '@/src/hooks/usePageTitle'
@@ -14,2 +15,3 @@
   const navigate = useNavigate()
+  const getPath = useRoleBasedPath()
   const [selectedCourseId, setSelectedCourseId] = useState<string>('')
@@ -81,3 +83,3 @@
             <button
-              onClick={() => navigate('/app/teacher/course-builder')}
+              onClick={() => navigate(getPath('/app/teacher/course-builder', '/app/admin/course-builder'))}
               className="flex items-center gap-2 px-4 py-2.5 rounded-xl font-medium text-white bg-indigo-600 hover:bg-indigo-700 transition-all shadow-sm"
@@ -101,3 +103,3 @@
               if (selectedCourseId) {
-                navigate(`/app/teacher/course-analytics?courseId=${selectedCourseId}`)
+                navigate(`${getPath('/app/teacher/course-analytics', '/app/admin/course-analytics')}?courseId=${selectedCourseId}`)
               }
