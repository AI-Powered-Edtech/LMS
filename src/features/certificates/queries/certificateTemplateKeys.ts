import { createQueryKeys } from '@/shared/lib/queryKeys'

/**
 * React Query key factory for certificate templates.
 * Phase 36C — Certificate Template Customization
 */
export const certTemplateKeys = createQueryKeys('cert-templates')
