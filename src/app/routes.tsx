/**
 * Thin re-export — all route logic lives in src/app/routes/.
 * This file exists to preserve the existing import path used by App.tsx:
 *   import { AppRoutes } from './app/routes'
 */
export { AppRoutes } from './routes/index'
