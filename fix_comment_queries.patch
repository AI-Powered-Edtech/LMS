--- src/features/discussions/hooks/useCommentQueries.ts
+++ src/features/discussions/hooks/useCommentQueries.ts
@@ -29,2 +29,5 @@
     mutationFn: ({ threadId, text }: { threadId: string; text: string }) => {
+      // Throw error to trigger toast since DB doesn't support assignment comments
+      throw new Error('NOT_IMPLEMENTED')
+      /*
       if (!user) throw new Error('Unauthenticated')
@@ -32,2 +35,3 @@
     },
+    /*
     onSuccess: (data, { threadId, text }) => {
@@ -45,3 +49,7 @@
     },
+    */
     onError: (err) => {
-      captureError(err, { context: 'useAddComment' })
+      useToast.getState().addToast({
+        type: 'info',
+        message: 'Fitur komentar tugas sedang dalam pengembangan.',
+      })
     },
