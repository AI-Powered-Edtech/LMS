--- src/features/discussions/components/forum/PostItem.tsx
+++ src/features/discussions/components/forum/PostItem.tsx
@@ -4,2 +4,3 @@
 import { useAuth } from '@/src/contexts/AuthContext'
+import { useToast } from '@/src/hooks/useToast'
 
@@ -19,2 +20,3 @@
   const { profile } = useAuth()
+  const addToast = useToast(s => s.addToast)
   const [expanded, setExpanded] = useState(false)
@@ -161,2 +163,3 @@
                   {isTeacher && (
-                    <button className="w-full text-left px-4 py-2 text-sm font-medium text-blue-600 hover:bg-blue-50 dark:hover:bg-blue-900/20 flex items-center gap-2 border-b border-slate-100 dark:border-slate-700">
+                    <button 
+                      onClick={() => addToast({ type: 'info', message: 'Fitur Push ke GCR segera hadir.' })}
+                      className="w-full text-left px-4 py-2 text-sm font-medium text-blue-600 hover:bg-blue-50 dark:hover:bg-blue-900/20 flex items-center gap-2 border-b border-slate-100 dark:border-slate-700"
+                    >
                       <Share2 className="w-4 h-4" /> Push ke GCR
@@ -218,2 +221,8 @@
           <button
+            onClick={() => {
+              navigator.clipboard.writeText(`${window.location.origin}/#/forum/${post.id}`)
+              addToast({ type: 'success', message: 'Tautan disalin ke papan klip.' })
+            }}
             className="flex items-center gap-2 text-slate-500 dark:text-slate-400 hover:text-blue-600 dark:hover:text-blue-400 font-medium transition-colors"
           >
