# TODO: Billing Module

> Generated by project analysis on 2026-03-31

## Database Tables Exist

The following billing-related tables are already in the database (via `000_baseline.sql`):

| Table      | Purpose                     |
| ---------- | --------------------------- |
| `invoices` | Invoice records             |
| `payments` | Payment transaction records |

## What's Missing

There is **no frontend feature module** for billing. To implement a minimal billing module:

### Phase 1: Basic Module

- [ ] Create `src/features/billing/` module
- [ ] Invoice list view (admin)
- [ ] Payment history view (admin)
- [ ] API service layer for invoices/payments queries

### Phase 2: Integration

- [ ] Connect to payment gateway (if applicable)
- [ ] Invoice PDF generation
- [ ] Billing notifications
- [ ] Subscription management (if SaaS model)

### Phase 3: Student-Facing

- [ ] Student payment status
- [ ] Payment reminders
- [ ] Receipt downloads

## Notes

- Check RLS policies on `invoices` and `payments` tables before building
- Verify `tenant_id` and `auto_set_tenant_id()` trigger are set up
- Consider integration with `announcements` for payment reminders

---

_Document generated during Phase 21 cleanup._
